package com.scautils

// trait Computation[_] {
//   def apply[A,B](in: A): B
// }

// object ZeroSafeComprehension {
//   def appl(items: Iterable[Computation[_]])(f:
// Computation#apply)(result: [A, B, C, D,...] => K)
// }

// // Then, we could do something like....

// ZeroSafeComprehension(
//   name <- person.name,
//   age  <- person.age
// )(fromJson(_))(Person.apply)

// object Fold {

//  // Folds through each item and applys f to produce K
//  def apply => Iterable[Composable] => (f: A..D => E..H) => K

// }
